import type { ReactNode } from 'react';

export const metadata = {
  title: 'Contact Hired Billing Support | Hired Billing Support',
  description: 'Get in touch with Hired Billing Support to discuss revenue cycle support, staffing, and operational solutions for your organization.',
};

export default function ContactLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
