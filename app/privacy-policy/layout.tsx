import type { ReactNode } from 'react';

export const metadata = {
  title: 'Privacy Policy | Hired Billing Support',
  description: 'Read how Hired Billing Support collects, uses, and protects information shared through our healthcare operations website.',
};

export default function PrivacyPolicyLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
