import type { ReactNode } from 'react';

export const metadata = {
  title: 'ROI Calculator | Hired Billing Support',
  description: 'Estimate the cost savings and operational impact of outsourcing healthcare billing and administrative support with our ROI calculator.',
};

export default function CostROICalculatorLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
