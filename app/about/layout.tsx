import type { ReactNode } from 'react';

export const metadata = {
  title: 'About Hired Billing Support | Healthcare Ops Partner',
  description: 'Learn how HBS supports healthcare teams with embedded billing, RCM, credentialing, coding, analytics, and operations support.',
};

export default function AboutLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
