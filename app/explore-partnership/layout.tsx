import type { ReactNode } from 'react';

export const metadata = {
  title: 'Explore Healthcare Partnership Models | HBS',
  description: 'Discuss a custom healthcare operations partnership for billing, RCM, credentialing, payer support, analytics, or backend execution.',
};

export default function ExplorePartnershipLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
