import type { ReactNode } from 'react';

export const metadata = {
  title: 'Find the Right Healthcare Support Service | HBS',
  description: 'Not sure where the workflow is breaking? Match your billing, AR, denial, credentialing, or operations problem to the right HBS service.',
};

export default function FindServiceLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
