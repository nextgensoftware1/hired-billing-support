import type { ReactNode } from 'react';

export const metadata = {
  title: 'Case Studies | Hired Billing Support',
  description: 'Read real case studies showing how Hired Billing Support improves operations, collections, and staffing outcomes for healthcare practices.',
};

export default function CaseStudyLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
